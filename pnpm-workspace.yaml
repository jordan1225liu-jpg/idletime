packages:
  - "apps/*"
  - "packages/*"

# pnpm 10+ 安全機制:明確允許 install scripts
# - esbuild:tsx 的相依,下載原生 binary
# - @prisma/*:下載 query engine native binary
allowBuilds:
  esbuild: true
  '@prisma/client': true
  '@prisma/engines': true
  prisma: true
